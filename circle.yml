machine:
  ruby:
    version: 2.3.1

dependencies:
  override:
    - ./bin/setup

test:
  override:
    - bundle exec jekyll hyde
    - bundle exec jekyll build
    # - bundle exec htmlproofer ./_site --allow-hash-href

deployment:
  alpha:
    branch: develop
    commands:
      - cd _site
      - git checkout master
      - git push
      # - ./bin/cideploy.sh
